---
import Layout from "@/layouts/Layout.astro";
import Profile from "@/assets/imagen.svg";
import { language } from "@/utils/data";
import { socialLinks } from "@/utils/data";
import { studies } from "@/utils/data";
import { skills } from "@/utils/data";
import Tooltip from "@/lib/tooltip.astro";
---

<Layout title="Acerca de mí | Portfolio">
    <section class="py-8 md:py-24 px-6 md:px-12 lg:px-48">
        <div class="min-h-screen flex flex-col md:flex-row">
            <!-- Columna Izquierda - Avatar y Datos -->
            <div class="flex flex-col items-center md:w-1/3 mb-10 md:mb-0">
                <div
                    class="w-42 h-42 overflow-hidden rounded-full border-4 border-white/10 mb-6"
                >
                    <img
                        src={Profile.src}
                        alt="Miller Mosquera"
                        class="w-full h-full object-cover"
                    />
                </div>

                <div class="items-center">
                    <p class="text-2xl mb-2">Ingeniero de Sistemas</p>
                    <p class="dark:text-neutral-500 text-neutral-500">
                        Escribo software y esas cosas. 🫨
                    </p>
                    <span
                        class="flex justify-center mb-6 dark:text-neutral-500 text-neutral-500"
                        >Colombia/Cali</span
                    >
                </div>

                <div class="flex space-x-2 text-sm">
                    <p class="flex gap-6 flex-wrap">
                        {
                            language.map((lang) => (
                                <span class="px-3 py-1 bg-white-200 rounded-md border border-white/10 shadow-xl shadow-black/25 backdrop-blur-[-10px]">
                                    {lang.text}
                                </span>
                            ))
                        }
                    </p>
                </div>
            </div>

            <!-- Columna Derecha - Información -->
            <div class="md:w-2/3 md:pl-10 py-14">
                <!-- Título -->

                <h1 class="text-5xl font-bold">
                    Miller Mosquera
                    <span class="absolute">
                        <Tooltip />
                    </span>
                </h1>
                <p class="mb-4 text-2xl dark:text-neutral-500 text-neutral-500">
                    Backend Dev
                </p>
                <div class="flex space-x-2 mb-6">
                    {
                        socialLinks.map((link) => (
                            <a
                                href={link.url}
                                class="py-1 px-3 flex from-white/10 to-transparent border border-white/10 shadow-xl shadow-black/25 duration-500 rounded-[10px] backdrop-blur-[10px] "
                            >
                                <img
                                    src={link.image}
                                    alt={link.text}
                                    class="w-4 h-4 mr-2 mt-1"
                                />
                                {link.text}
                            </a>
                        ))
                    }
                </div>

                <!-- Descripción -->

                <p
                    class="mb-16 leading-relaxed dark:text-neutral-500 text-neutral-500"
                >
                    Un Apasionado por la tecnología y formado como <strong
                        class="text-white">Ingeniero de Sistemas,</strong
                    > actualmente me especializo en desarrollo <strong
                        class="text-white">Back-end</strong
                    >. Comprometido con el diseño y la implementación de
                    soluciones tecnológicas robustas, escalables y orientadas al
                    alto rendimiento. Actualmente busco seguir creciendo de
                    manera profesional, aportando valor en proyectos que
                    representen un verdadero reto técnico y generen impacto real
                    en los usuarios y en el negocio.
                </p>

                <!-- Experiencia de Trabajo
                <div>
                    <h2 class="text-3xl font-bold mb-8">Work Experience</h2>
                    <div class="">
                        {
                            workExperience.map((work) => (
                                <div class="flex justify-between items-center mb-4">
                                    <div class="flex flex-col">
                                        <h3 class="text-xl font-bold">
                                            {work.company}
                                        </h3>
                                        <span class="text-[#8dc7b7]">
                                            {work.position}
                                        </span>
                                    </div>
                                    <span class="text-sm mb-6">
                                        {work.duration}
                                    </span>                                    
                                </div>
                                
                                <p class="text-sm ">
                                    {(Array.isArray(work.description) ? work.description.join('\n') : (work.description ?? ' ')).split('\n').map((line) => (
                                        <div class="flex items-start">
                                            <span class="text-[#8dc7b7] mr-2 text-2">•</span>
                                            <span>{line}</span>
                                        </div>
                                    ))}
                                </p>
                            ))
                        }
                    </div>
                </div>
                -->

                <!-- Educación -->
                <div class="mb-16">
                    <h2 class="text-3xl font-bold mb-8">Educación</h2>
                    <div>
                        {
                            studies.map((study) => (
                                <div class="flex justify-between items-center mb-4">
                                    <div class="flex flex-col">
                                        <h3 class="text-xl font-bold">
                                            {study.institution}
                                        </h3>
                                        <span class=" dark:text-neutral-500 text-neutral-500">
                                            {study.degree}
                                        </span>
                                    </div>
                                    <span class="text-sm mb-6  dark:text-neutral-500 text-neutral-500">
                                        {study.duration}
                                    </span>
                                </div>
                            ))
                        }
                    </div>
                </div>
                <div>
                    <h2 class="text-3xl font-bold mb-8">Skills</h2>
                    <div>
                        <ul class="mt-2 flex flex-wrap">
                            {
                                skills.map((skill) => (
                                    <ul class="flex justify-between px-3 items-center mb-4">
                                        <div class="flex ">
                                            <h3 class="text-sm">
                                                {skill.text}
                                            </h3>
                                        </div>
                                    </ul>
                                ))
                            }
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </section>
</Layout>
